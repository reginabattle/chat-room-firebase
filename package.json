{
  "name": "chat-room",
  "version": "1.0.0",
  "description": "Simple chat room",
  "main": "app.js",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html --out-dir build --public-url ./ --no-source-maps --no-content-hash --no-cache"
  },
  "author": "Regina Battle",
  "license": "ISC",
  "devDependencies": {
    "babel-polyfill": "^6.26.0",
    "cssnano": "^4.1.10",
    "date-fns": "^2.12.0",
    "sass": "^1.26.5"
  }
}
